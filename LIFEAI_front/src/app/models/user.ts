export interface loginUser {
    email: string;
    password: string;
    message: 'usuario registrado com sucesso.';
}

export interface registroUser {
  username: string;
  email: string;
  password: string;
  message: 'usuario registrado com sucesso.';
}